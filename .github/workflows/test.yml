name: Test Schedule

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test çalıştı mı
        run: |
          echo "event=${{ github.event_name }}"
          date -u
